# Local MongoDB only — optional for development.
# API and client run via npm in server/ and client/.

services:
  mongodb:
    image: mongo:7.0
    container_name: suni-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
